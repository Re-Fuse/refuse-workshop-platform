%h3
  Highlights from today

%p You can highlight whatever you find important by selecting the text you like and clicking the button `highlight`


.fix
  %a.btn.btn-primary#save-text.fixed Highlight


  .well.col-sm-10.pull-right
    -# %h3 Word usage
    -# %ul
    -#   - Message.highlight_stats.each do |ind|
    -#     %li= "#{ind[0]} - #{ind[1]} times"

    %h3 Summary
    %ul.list-unstyled
      - User.all.each do |user|
        %h3= user.name
        - user.highlights.each do |message|
          - if message.value != 'Highlighted this: <span></span>'
            %blockquote.saying= message.value.sub!('Highlighted this:', '').html_safe

    %h3 Sroll Up To See All



:coffeescript
  $('#save-text').hide()
  $('#save-text').on 'click', ->
    Kolich.Selector.save()
